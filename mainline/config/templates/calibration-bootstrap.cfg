# Commissioning-only control values. This file allows cold bring-up and PID
# calibration without copying stale PID values from the vendor installation.
# The release validator refuses to approve a production image containing it.
[extruder]
control: watermark

[heater_bed]
control: watermark

# Syntax placeholder only. The commissioning lock prevents homing or motion
# until the owner has produced a measured release calibration in maintenance.
[probe]
z_offset: 0
